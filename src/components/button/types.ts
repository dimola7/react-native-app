export interface IButton {
    title: string;
    onPress: () => void;
    bgColor: any;
  }
  export interface IStyles {
    fontSize?: string,
    color?: string,
    backgroundColor?: string,
    width?: string;
    fontWeight?: string,
    borderRadius?: string,
    textAlign?: string,
    padding?: string,
}